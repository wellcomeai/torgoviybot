# МИНИМАЛЬНЫЙ набор БЕЗ проблемных зависимостей
# Только pure Python пакеты с готовыми wheels

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# WebSocket client - pure Python
websockets==12.0

# Telegram bot - стабильная версия
python-telegram-bot==21.0.1

# HTTP clients
httpx==0.27.0
aiohttp==3.9.5

# Basic utilities
python-dotenv==1.0.1
typing-extensions==4.10.0
async-timeout==4.0.3

# БЕЗ pydantic - заменим на dataclasses
# БЕЗ pydantic-settings - создадим свой класс настроек
