# МИНИМАЛЬНЫЙ набор для Python 3.13 на Render
# Только пакеты с готовыми wheel-файлами

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Configuration - совместимые с Python 3.13
pydantic==2.6.4
pydantic-settings==2.2.1

# WebSocket client - pure Python
websockets==12.0

# Telegram bot - совместимый
python-telegram-bot==21.0.1

# HTTP clients
httpx==0.27.0
aiohttp==3.9.5

# Basic utilities - только если wheel доступен
python-dotenv==1.0.1
typing-extensions==4.10.0
async-timeout==4.0.3

# Security - если нужен
# cryptography==42.0.5  # Закомментировано - может быть проблема

# БЕЗ numpy - заменим на встроенные функции Python
# БЕЗ psutil - не критично для работы
# БЕЗ orjson - используем стандартный json
